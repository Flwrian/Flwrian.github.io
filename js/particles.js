var particles = Particles.init({
    selector: '.background',
    color: '#DA0463',
    maxParticles: 150
});

window.onload = function () {
    Particles.init({
        selector:'.background'
    });
};